{
  "name": "waggle-viewer",
  "version": "0.1.0",
  "description": "Web viewer for waggle tokens — browse folders, render markdown, preview sites, view code/images/CSV/JSON",
  "type": "module",
  "bin": {
    "waggle-viewer": "src/index.ts"
  },
  "scripts": {
    "dev": "bun src/index.ts",
    "start": "bun src/index.ts",
    "test": "bun test"
  },
  "keywords": ["waggle", "viewer", "tokens", "content-addressed", "browser"],
  "license": "MIT",
  "dependencies": {
    "marked": "^15.0.0",
    "shiki": "^1.0.0",
    "csv-parse": "^5.6.0"
  },
  "devDependencies": {
    "@types/bun": "latest"
  },
  "engines": {
    "bun": ">=1.0.0"
  }
}
